/**
 * Shared strict-rectangle collision padding, in world pixels, applied around
 * collision/landmark rects when testing whether the player centre is blocked.
 * The value 12 is the radius the collision geometry requires.
 */
export const PLAYER_COLLISION_RADIUS = 12;
